#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/home/kwan-ai/Documents/github/personal/fieldops/node_modules/.pnpm/agent-cli-detector@0.1.5/node_modules/agent-cli-detector/node_modules:/home/kwan-ai/Documents/github/personal/fieldops/node_modules/.pnpm/agent-cli-detector@0.1.5/node_modules:/home/kwan-ai/Documents/github/personal/fieldops/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/home/kwan-ai/Documents/github/personal/fieldops/node_modules/.pnpm/agent-cli-detector@0.1.5/node_modules/agent-cli-detector/node_modules:/home/kwan-ai/Documents/github/personal/fieldops/node_modules/.pnpm/agent-cli-detector@0.1.5/node_modules:/home/kwan-ai/Documents/github/personal/fieldops/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../../../../agent-cli-detector/dist/cli.js" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../agent-cli-detector/dist/cli.js" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../../../../agent-cli-detector/dist/cli.js" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../../../../agent-cli-detector/dist/cli.js" "$@"
else
  exec node  "$basedir/../../../../agent-cli-detector/dist/cli.js" "$@"
fi
# cmd-shim-target=/home/kwan-ai/Documents/github/personal/fieldops/node_modules/.pnpm/@expo+cli@56.1.22_@expo+dom-webview@56.0.6_@expo+metro-runtime@56.0.18_expo-constants@5_674cc262757d14143c14ca198c6a01fb/node_modules/agent-cli-detector/dist/cli.js
